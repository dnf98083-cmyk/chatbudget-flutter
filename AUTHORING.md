# AUTHORING.md
> 본인만의 AI Agent 활용 방법론 — ChatBudget 프로젝트

**작성자**: dnf98083-cmyk  
**버전**: v0.1.0  
**최초 작성**: 2026-05-12

---

## 1. 이 파일의 목적

이 파일 하나로 다음을 부트스트랩한다:
- AI Agent에게 어떤 역할을 맡길지 (agent)
- 어떤 순서로 작업을 진행할지 (workflow)
- 어떤 규칙을 따를지 (rules)
- 반복 작업을 어떻게 자동화할지 (commands)

> 새 프로젝트를 시작할 때 이 파일을 AI Agent에게 먼저 보여주면
> 프로젝트 전체 구조와 문서를 자동으로 생성할 수 있다.

---

## 2. AI Agent 역할 정의

```
너는 내 개발 파트너야.
나는 What(무엇을)과 Why(왜)를 결정하고,
너는 How(어떻게)를 구현한다.

단, 내가 모르는 것은 반드시 선택형 질문으로 물어봐.
코드를 만든 후에는 내가 설명할 수 있도록 핵심 개념을 요약해줘.
```

---

## 3. 프로젝트 시작 워크플로우

새 프로젝트를 시작할 때 이 순서로 AI Agent에게 시킨다:

```
1단계: 비전 & 요구사항
"[주제 한 줄]을 만들고 싶다.
.planning/00-vision.md에 비전/목표/사용자 시나리오 3개 작성해줘.
.planning/01-requirements.md에 MoSCoW로 기능 분류해줘.
모호한 부분은 선택형 질문으로 물어봐."

2단계: WBS & 일정
".planning/02-wbs.md에 WBS를 명사형 산출물로 3단계까지 작성해줘.
.planning/04-schedule.md에 6주 일정으로 매핑해줘.
각 항목은 1~3일 단위로 추정 가능하게 해줘."

3단계: 기술 결정 (ADR)
"다음 3가지를 결정해야 해:
1) 모바일 프레임워크
2) 상태 관리
3) 로컬 DB
각각 대안 3개와 선택 이유를 ADR 형식으로 .planning/decisions/에 작성해줘."

4단계: 프로젝트 초기화
"[기술스택] 프로젝트를 생성하고
feature-based 폴더 구조로 초기화해줘.
핵심 패키지를 pubspec.yaml에 추가하고 설명해줘."

5단계: 기능 구현
"[기능명]을 구현해줘.
구현 후 내가 면접에서 설명할 수 있도록 핵심 개념 3줄 요약해줘."
```

---

## 4. 내가 정한 규칙 (Rules)

### 코드 규칙
- feature-based 폴더 구조 고수 (`lib/features/`, `lib/core/`)
- 모든 DB 접근은 `DbHelper` 싱글톤을 통해서만
- 화면(Screen)과 로직(Parser, Helper)은 반드시 분리

### AI Agent 사용 규칙
- AI가 만든 코드는 반드시 직접 읽고 핵심 로직 파악 후 다음 단계 진행
- 모르는 부분이 생기면 그 자리에서 AI에게 설명 요청
- 결정 사항은 반드시 ADR로 남긴다

### 문서 규칙
- 모든 기술 선택에는 ADR 작성
- BONUS.md에 가산점 진행 상황 매주 업데이트
- 커밋 메시지: `feat/fix/docs/refactor: 내용`

---

## 5. 반복 명령어 모음 (Commands)

### 주간 진행 보고서 생성
```
이번 주 진행 상황을 .planning/04-schedule.md 기준으로 점검해줘.
완료된 항목, 진행 중, 미완료를 정리하고
다음 주 우선순위 3개를 추천해줘.
```

### 모르는 코드 이해
```
이 코드를 내가 면접관에게 설명한다고 생각하고
1) 이게 뭐 하는 코드인지
2) 왜 이렇게 구현했는지
3) 다른 방법은 없었는지
3줄로 설명해줘.
```

### 발표 Q&A 대비
```
이 프로젝트의 발표자라면 교수님이 물어볼
기술적 질문 5개와 답변을 만들어줘.
특히 플랫폼 선택, 앱 구조, 빌드/배포, 테스트에 집중해줘.
```

---

## 6. 실제 활용 사례 (ChatBudget 프로젝트)

| 작업 | AI Agent 활용 | 결과 |
|---|---|---|
| 기획 문서 5개 | Claude Code에게 비전/요구사항/WBS/일정/BONUS 일괄 생성 요청 | 30분 내 완성 |
| ADR 3개 | Flutter/Riverpod/SQLite 선택 이유를 대안 비교 포함해 작성 요청 | 기술 질문 즉답 가능 |
| GitHub 저장소 생성 | `gh repo create` 명령 자동 실행 | 수동 작업 0 |
| Flutter 앱 전체 구조 | feature-based 구조로 4개 화면 + 파서 + DB 자동 생성 | 2시간 내 작동하는 프로토타입 |
| 자연어 파서 | "스타벅스 6000원" → 날짜/금액/카테고리 추출 로직 구현 요청 | 단위 테스트까지 자동 생성 |

---

## 7. 이 방법론으로 배운 것

1. **AI에게 맡기되, 이해는 내가** — 코드를 받으면 바로 "이게 뭐야?" 질문
2. **결정은 ADR로** — AI가 준 선택지 중 하나를 고를 때 이유를 남겨야 나중에 설명 가능
3. **문서 먼저, 코드 나중** — `.planning/` 문서가 있으면 AI가 방향을 잃지 않음
4. **명령어를 모아두면 재사용 가능** — 이 파일의 Commands 섹션이 다음 프로젝트에서도 자산이 됨
